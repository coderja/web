//indexOf方法
function findIndex(msg,array){
				  var index=-1
				  for(var i=0;i<array.length;i++){
					  if(msg==array[i]){
						  index = i
						}
					}
				   return index
				}

function findIndex(msg,array){
   var index=-1
   for(var i=0;i<array.length;i++){
      if(msg=array[i])
		  index=i;
   }
    return index;
}
